## 概要

AgentHubサイドバーで、別セッションの状態更新によって入力中の日本語IME変換が確定・中断される不具合を解消する。

セッション一覧の更新中も、各セッションのテキスト入力、IME composition、カーソル位置、選択範囲、フォーカスを維持する。

## 背景

あるセッションが実行中の状態で、別セッションのテキストボックスへ日本語を入力すると、状態更新のタイミングで変換途中の文字が意図せず確定する。例えば「日本語」へ変換しようとしている途中に更新が入ると、「にほんご」のような未変換文字列になる。

`src/presentation/session-webview-provider.ts` のWebviewは、`sessions`メッセージを受信するたびに `render()` を呼び出す。`render()` は既存のdraft値とフォーカス対象を読み取った後、`root.replaceChildren()` で全セッションのDOMを破棄し、`textarea`を新規作成している。

文字列の値は復元されるが、IMEのcomposition状態はDOM要素に紐づくため復元できない。別セッションの進捗イベントでも一覧全体が更新されることから、入力対象とは無関係なセッション更新がIME確定を引き起こしている。

## 方針

- セッション更新時に一覧全体のDOMを破棄せず、セッションIDをキーとして既存カードと入力要素を再利用する
- セッションの状態、現在アクティビティ、操作ボタンなど、変更された表示だけを差分更新する
- `textarea`は対象セッションが削除されるまで同一DOM要素を維持し、入力値をサーバー側snapshotで上書きしない
- `compositionstart`、`compositionend`でIME変換状態を追跡し、composition中は入力要素を交換・再生成しない
- カーソル位置、選択範囲、スクロール位置、フォーカスを維持する
- 並び替えでは既存カードをDOM上で移動し、カードや`textarea`を再生成しない
- 新規セッションは追加、削除済みセッションは除去する形で差分を反映する

採らない方針:

- 状態更新を長時間停止してIME入力を守る方法は採らない。実行中セッションの進捗表示は継続する
- draft文字列だけを退避・復元する現行方式の延長では、IME compositionを復元できないため解決策としない
- 日本語IMEだけを例外扱いせず、中国語・韓国語等を含むcompositionイベント全般を保護する

## 制約

- WebviewのCSPと既存のVanilla JavaScript構成を維持する
- 別セッションの状態、承認待ち、完了などの更新通知を遅延させない
- Enter送信はcomposition中に実行せず、変換確定後のEnterだけを送信操作として扱う
- キーボードによるセッション並び替えと既存のフォーカス移動を壊さない

## 非対象

- テキスト入力UIのデザイン変更
- Codexへの送信プロトコル変更
- 自由入力型`requestUserInput`の実装
- セッション詳細画面全体の再設計

## 受け入れ条件

- セッションAが継続的に状態更新している間に、セッションBで「日本語」をIME入力・変換しても、未変換の「にほんご」で意図せず確定されない
- composition中に複数回`sessions`更新を受信しても、同じ`textarea` DOM要素、入力値、カーソル位置、選択範囲、フォーカスが維持される
- composition中のEnterでメッセージが送信されず、変換確定後のEnterで一度だけ送信される
- 入力中に別セッションが開始、進行、承認待ち、完了へ遷移しても、その表示は更新される
- 入力中にセッション順序が変更されてもdraftとIME入力が維持される
- 日本語IMEを含むcompositionイベントの回帰テストを追加する
- 既存のセッション送信、並び替え、承認、中断、詳細表示の操作が回帰しない
